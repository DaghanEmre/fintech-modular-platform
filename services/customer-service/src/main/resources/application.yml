spring:
  application:
    name: customer-service
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

# Common Actuator settings (base-path rarely changes)
# If you need profile-specific actuator paths, move this to profile files
management:
  endpoints:
    web:
      base-path: /actuator
